[core]
    autocrlf = true
    whitespace = cr-at-eol
[diff]
    tool = p4merge
[difftool]
    prompt = false
[merge]
    tool = p4merge
[mergetool]
    keepBackup = false
[mergetool "p4merge"]
    trustExitCode = true

# vim: set ft=gitconfig :
